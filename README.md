# Telegram Channel Summary Bot

Telegram бот для автоматического создания саммари (summary) сообщений из отслеживаемых Telegram каналов. Поддерживает публичные и приватные каналы, использует OpenRouter API для генерации саммари.

## Возможности

- ✅ Отслеживание публичных и приватных Telegram каналов
- 🤖 Автоматическая генерация саммари через OpenRouter API (Claude, GPT и др.)
- ⏰ Настраиваемый период отправки (раз в день, раз в 3 дня, раз в неделю)
- 📝 Ручной запуск генерации саммари по команде
- 💾 Хранение данных в SQLite базе
- 🔒 Поддержка приватных каналов через Telegram user account

## Требования

- Python 3.9+
- Telegram Bot Token (получить у @BotFather)
- Telegram API credentials (API ID и API Hash с https://my.telegram.org/apps)
- OpenRouter API Key (получить на https://openrouter.ai/)

## Установка

### 1. Клонирование и создание виртуального окружения

```bash
# Перейти в директорию проекта
cd /home/aleksander/code/summary

# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Скопируйте файл `.env.example` в `.env` и заполните все значения:

```bash
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

Необходимо заполнить:

- **BOT_TOKEN**: Токен вашего бота (получить у @BotFather в Telegram)
- **API_ID** и **API_HASH**: Credentials для Telegram API (получить на https://my.telegram.org/apps)
- **OPENROUTER_API_KEY**: API ключ OpenRouter (получить на https://openrouter.ai/)
- **OPENROUTER_MODEL**: Модель для генерации саммари (по умолчанию `anthropic/claude-3.5-sonnet`)

### 4. Первый запуск и авторизация

При первом запуске бота вам нужно будет авторизовать Telegram user account:

```bash
python main.py
```

Бот попросит:
1. Ввести номер телефона
2. Ввести код подтверждения из Telegram
3. (При необходимости) Ввести пароль 2FA

Это нужно для того, чтобы бот мог читать сообщения из каналов (в том числе приватных).

**⚠️ ВАЖНО:** Используйте аккаунт, который подписан на все приватные каналы, которые вы хотите отслеживать.

## Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку по командам
- `/add @channelname` - Добавить канал для отслеживания
- `/remove @channelname` - Удалить канал из отслеживания
- `/list` - Показать список отслеживаемых каналов
- `/period` - Настроить период автоматической отправки саммари
- `/summary` - Получить саммари по всем каналам прямо сейчас

### Примеры использования

```
/add @durov
/add @python_news
/list
/period  # Выбрать период из предложенных вариантов
/summary  # Получить саммари сейчас
```

## Структура проекта

```
summary/
├── main.py              # Точка входа приложения
├── bot/                 # Пакет Telegram бота
│   ├── core.py          # Инициализация бота (SummaryBot)
│   ├── commands.py      # Команды бота (/start, /add и др.)
│   ├── callbacks.py     # Обработчики callback-кнопок
│   ├── summarizer.py    # Генерация саммари через OpenRouter
│   ├── helpers.py       # Вспомогательные функции
│   └── messages.py      # Шаблоны сообщений
├── client.py            # Telegram клиент для чтения каналов (Telethon)
├── scheduler.py         # Планировщик периодических задач
├── database.py          # Работа с базой данных (SQLite)
├── config.py            # Загрузка конфигурации
├── requirements.txt     # Зависимости Python
├── .env.example         # Пример конфигурации
├── .env                 # Ваша конфигурация (не в git)
├── docs/                # Дополнительная документация
│   ├── QUICKSTART.md    # Быстрый старт
│   ├── EXAMPLES.md      # Примеры использования
│   ├── CHECKLIST.md     # Чеклист запуска
│   └── PROJECT_STRUCTURE.md  # Подробная архитектура
└── README.md            # Эта документация
```

## Как работает бот

1. **Отслеживание каналов**: Пользователь добавляет каналы командой `/add`
2. **Периодическая проверка**: Scheduler каждый час проверяет, каким пользователям пора отправить саммари
3. **Чтение сообщений**: Через Telethon читаются сообщения из каналов за указанный период
4. **Генерация саммари**: Сообщения отправляются в OpenRouter API для генерации краткого саммари
5. **Отправка пользователю**: Саммари отправляется пользователю в Telegram

## Автоматическая отправка

Бот автоматически отправляет саммари на основе настроенного периода:

- **Раз в день** (по умолчанию): Саммари за последние 24 часа
- **Раз в 3 дня**: Саммари за последние 3 дня
- **Раз в неделю**: Саммари за последние 7 дней

Scheduler проверяет необходимость отправки каждый час.

## Работа с приватными каналами

Для доступа к приватным каналам:

1. Убедитесь, что Telegram аккаунт, который вы используете для авторизации (при первом запуске), подписан на нужные приватные каналы
2. Добавьте канал командой `/add @channelname`
3. Бот проверит доступ и добавит канал в отслеживание

## Остановка бота

Нажмите `Ctrl+C` в терминале. Бот корректно завершит все процессы.

## Запуск в фоне (Linux)

Для постоянной работы бота рекомендуется использовать systemd service или screen/tmux:

### Вариант 1: screen

```bash
screen -S telegram-bot
source venv/bin/activate
python main.py
# Нажать Ctrl+A, затем D для отсоединения
# Для возврата: screen -r telegram-bot
```

### Вариант 2: systemd service

Создайте файл `/etc/systemd/system/telegram-summary-bot.service`:

```ini
[Unit]
Description=Telegram Summary Bot
After=network.target

[Service]
Type=simple
User=aleksander
WorkingDirectory=/home/aleksander/code/summary
ExecStart=/home/aleksander/code/summary/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-summary-bot
sudo systemctl start telegram-summary-bot
sudo systemctl status telegram-summary-bot
```

## Устранение неполадок

### Бот не может прочитать канал

- Убедитесь, что аккаунт подписан на канал (для приватных)
- Проверьте правильность username канала (без пробелов)
- Проверьте, что канал действительно существует

### Ошибка авторизации Telegram

- Убедитесь, что API_ID и API_HASH правильные
- Проверьте, что номер телефона введен в международном формате (+...)
- При использовании 2FA убедитесь, что пароль правильный

### Ошибка генерации саммари

- Проверьте, что OPENROUTER_API_KEY корректный
- Убедитесь, что на балансе OpenRouter есть средства
- Проверьте доступность модели (некоторые модели могут требовать дополнительных прав)

## Лицензия

MIT License

## Автор

Created with Claude Code
